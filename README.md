# Dream Home 3D

A single-page interactive 3D home-design prototype built with Three.js.

## Local Preview

Open `index.html` directly in a browser, or run:

```bash
npm install
npm start
```

## Deployment

This repository is configured as a static Vercel project. No build step is required.
